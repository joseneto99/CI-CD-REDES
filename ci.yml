name: CI/CD - Network Automation Demo

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: 🧩 Checkout do código
        uses: actions/checkout@v3

      - name: 🐳 Configurar Docker
        uses: docker/setup-buildx-action@v3

      - name: 🔨 Build da imagem Docker
        run: docker build -t network-demo .

      - name: 🚀 Executar container e testar conectividade
        run: docker run network-demo

      - name: ✅ Finalização
        run: echo "Pipeline concluído com sucesso!"
